{
  "private": true,
  "devDependencies": {
    "@types/bun": "^1",
    "prettier": "^3",
    "prettier-plugin-organize-imports": "^4",
    "tree-sitter-cli": "^0.24"
  },
  "scripts": {
    "clone": "git clone --depth=1 --branch=v0.0.0-0.nightly.2024.10.30 https://github.com/carbon-language/carbon-lang && rm -rf carbon-lang/.git",
    "generate": "tree-sitter generate",
    "test": "tree-sitter test --update"
  }
}
